name: Vessel
version: "${version}"
main: org.maboroshi.vessel.Vessel
loader: org.maboroshi.vessel.Loader
description: Easily capture and store any entity in portable items, allowing you to move and release them anywhere.
author: Maboroshi
website: https://github.com/MaboroshiKobo/Vessel
api-version: "26.1"
folia-supported: true
dependencies:
  server:
    PlaceholderAPI:
      load: BEFORE
      required: false
    Nexo:
      load: BEFORE
      required: false
    MythicMobs:
      load: BEFORE
      required: false
    WorldGuard:
      load: BEFORE
      required: false
    Towny:
      load: BEFORE
      required: false